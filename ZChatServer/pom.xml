<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.11</version>
        <relativePath/>
    </parent>

    <groupId>com.yhj</groupId>
    <artifactId>ZChat</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>ZChat</name>
    <description>ZChat gateway (ZSP / Netty / JNI)</description>

    <properties>
        <java.version>17</java.version>
        <!-- JDK 24+（含 Linux 上的 JDK 25）追加 JVM 参数；Maven 跑测试/run 时生效。纯 java -jar 须在环境变量中自行带上，见 native/linux-x64/README.md -->
        <jdk24.jvm.extra.args></jdk24.jvm.extra.args>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>io.netty</groupId>
            <artifactId>netty-all</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>jdk24-jvm-warnings</id>
            <activation>
                <jdk>[24,)</jdk>
            </activation>
            <properties>
                <!-- Netty 堆外内存：JEP 498；见 https://netty.io/wiki/java-24-and-sun.misc.unsafe.html -->
                <!-- classpath 上 JNI System.load：须允许 unnamed 模块原生访问（NativeLibraryLoader） -->
                <jdk24.jvm.extra.args>--sun-misc-unsafe-memory-access=allow --enable-native-access=ALL-UNNAMED</jdk24.jvm.extra.args>
            </properties>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- 仅追加 JVM 参数；若日后接入 JaCoCo，请按 Spring Boot 文档合并 @{argLine} -->
                    <argLine>${jdk24.jvm.extra.args}</argLine>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.ztrust.zchat.boot.ZChatApplication</mainClass>
                    <jvmArguments>${jdk24.jvm.extra.args}</jvmArguments>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>

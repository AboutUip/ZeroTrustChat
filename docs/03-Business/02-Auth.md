# 认证安全机制技术规范

## 一、限流规则

```
MM1 认证计数器:
IP级别: 5次/分钟
用户级别: 10次/分钟
```

## 二、封禁机制

### 2.1 动态封禁策略

封禁时长根据失败次数动态计算：

```
封禁时长 = min(失败次数 × 基础时长, 最大封禁时长)
```

| 参数 | 值 | 说明 |
|------|-----|------|
| 基础时长 | 15分钟 | 每次失败累加 |
| 最大时长 | 24小时 | 封禁上限 |
| 封禁窗口 | 24小时 | 窗口期内连续计算 |
| 失败计数阈值 | 5次 | 触发封禁 |

> **与 第2.1节 公式**：第2.1节 为概念式表述；**实现与验收以 第2.2节 封禁矩阵表为准**（当前 C++ 按表取值）。

### 2.2 封禁矩阵

| 连续失败次数 | 封禁时长 |
|:-------------:|:--------:|
| 5 | 15分钟 |
| 6 | 30分钟 |
| 7 | 45分钟 |
| 8 | 1小时 |
| 9 | 1小时15分钟 |
| 10 | 1小时30分钟 |
| 11+ | 最大24小时 |

### 2.3 封禁存储

| 项目 | 要求 |
|------|------|
| 存储位置 | MM1 内存 |
| 索引键 | userId + IP |
| 重启丢失 | 是 |
| 日志记录 | 否 |

### 2.4 解封机制

| 方式 | 说明 |
|------|------|
| 自动解封 | 封禁时长到期自动解除 |
| 手动解封 | 管理员强制解除 |
| 重启不解封 | 服务重启保持封禁状态 |

> **与 第五节 的关系**：第五节写明计数器/封禁存 MM1 内存且**服务重启状态丢失**。第2.4节「重启不解封」指**单次进程生命周期内**封禁不因其它非重启事件自动清零；**进程重启后**内存态丢失，与 第五节 一致。

## 三、错误码

| 错误码 | 说明 |
|--------|------|
| E_AUTH_RATE_LIMIT | 频率超限 |
| E_AUTH_LOCKED | 已封禁 |
| E_AUTH_FAILED | 认证失败 |

## 四、认证成功

```
成功 → 重置MM1计数器 → 返回sessionId
```

## 五、安全原则

- 计数器/封禁存MM1内存
- 服务重启 → 状态丢失
- 不记录任何日志
- Level 2 销毁

## 六、参数汇总

| 参数 | 值 |
|------|-----|
| IP级限流 | 5次/分钟 |
| 用户级限流 | 10次/分钟 |
| 封禁基础时长 | 15分钟 |
| 封禁最大时长 | 24小时 |
| 封禁窗口 | 24小时 |
| 失败计数阈值 | 5次 |

## 七、C++ 实现落点（`ZChatIM`）

### 7.1 不透明凭证（`Auth` 的 `token`）

| 规则 | 说明 |
|------|------|
| 最小长度 | **`AUTH_OPAQUE_CREDENTIAL_MIN_BYTES` = 32**（`Types.h`），与 API key / 服务端票据常见下限一致 |
| 禁止全零 | 全 **0** 的 **32+** 字节 **拒绝** |
| 禁止与 userId 混淆 | 若 **`token.size() == userId.size()`** 且 **`ConstantTimeCompare`** 全相等则拒绝；在 **`AUTH_OPAQUE_CREDENTIAL_MIN_BYTES=32`** 下与 16B **`userId`** 不会同长，保留作防御性契约 |

JWT/HMAC 等应由**服务端**签发；客户端将**完整二进制 token**（≥32B）传入 JNI **`Auth`**。

### 7.2 组件表

| 组件 | 路径 | 说明 |
|------|------|------|
| `AuthSessionManager` | `include/mm1/managers/AuthSessionManager.h`、`src/mm1/managers/AuthSessionManager.cpp` | 会话表；**用户 10 次/分钟**、**IP 5 次/分钟**（仅当 `Auth` 传入非空 `clientIp`）；封禁键为 `userId` 或 `userId‖clientIp`；失败计数窗口 **24h**，封禁时长按 **第2.2节 矩阵**；成功清空对应限流队列与封禁项；`VerifySession`/`DestroySession` 移除会话项时对 `userId` 字段做内存清零（Level 2 语义的一部分）。**`ClearAllSessions`**：清空全部会话并对每条做 `userId` 清零，同时清空限流队列与封禁表（**进程内**）。**`VerifyCredential`**：按 **第7.1节** 校验形状与退化情况（**非**服务端密码学证明；真票据须由上层保证不可伪造）。**`JniBridge::EmergencyWipe` / `MM1::EmergencyTrustedZoneWipe`** 会清空会话表（及 **MM1** 其它进程内态，见 **`01-JNI.md`**）。 |

**限流计数语义**：每次进入 `Auth` 且在通过封禁检查后、凭证校验前记入 1 分钟滑动窗口；**含**后续凭证失败（与「每分钟尝试次数」一致）。**例外**：凭证**已通过**但仅因**会话表满**或 **sessionId 随机数生成失败**而拒绝时，会撤销当次已入队的限流计数，避免误伤合法用户（不属于「认证失败」语义）。

### 7.3 与「注册 / 忘记密码」（B1）及初始化顺序

| 项 | 说明 |
|----|------|
| **无注册 / 重置密码 JNI** | 开户、找回密码由 **App + HTTPS 业务域**完成；客户端将 **`userId`（16B）** 与服务端签发的 **`token`（≥32B 等，见上表）** 传入 JNI **`auth`**。详述见 **`ZChatIM/docs/JNI-API-Documentation.md` §0.5.1.1**。 |
| **须先 `Initialize` 再 `Auth`** | **`JniBridge::Auth`** 在桥接 **`m_initialized` 为 false** 时直接返回空会话（**`ZChatIM/src/jni/JniBridge.cpp`**），不会进入 **`AuthSessionManager`**。 |
| **`LocalAccountCredentialManager`** | 仓库内存在本地口令相关 C++，**当前未**接入 **`JniInterface`**；**不**改变「B1 = 业务 HTTPS + `auth`」的 JNI 边界。 |
